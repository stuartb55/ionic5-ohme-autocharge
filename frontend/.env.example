# Optional. Only needed when the API is NOT served from the same origin.
# Leave unset in production (nginx proxies /api to the backend).
# VITE_API_BASE=http://localhost:8000

# Dev-only: where `vite dev` proxies /api requests to.
# VITE_API_PROXY=http://localhost:8000
