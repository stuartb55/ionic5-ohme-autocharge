# Machine-specific paths resolved at run time — regenerated locally, not shared.
.graphify_python
.graphify_root
cache/

# Dated archive snapshots written by `graphify update` (created on every run by
# the auto-update hook) — redundant duplicates of the top-level graph, would
# accumulate one per day and bloat the repo.
[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
